FLASK_APP=run.py
FLASK_ENV=development
SECRET_KEY=change-me-in-production

# GCP VM: MySQL listens on 127.0.0.1 (same machine as the app).
# Use a dedicated database — do not use the LeadPilot DB.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=leadpilot
MYSQL_PASSWORD=your-mysql-password
MYSQL_DATABASE=crypto_signals

# Optional full URL overrides individual MYSQL_* vars
# DATABASE_URL=mysql+pymysql://leadpilot:password@127.0.0.1:3306/crypto_signals

TIMEZONE=UTC
SCANNER_INTERVAL_SECONDS=60
DEFAULT_TIMEFRAME=1H
EXCHANGE=binance
LOG_LEVEL=INFO
DEBUG_MODE=false
ENABLE_SCHEDULER=false
